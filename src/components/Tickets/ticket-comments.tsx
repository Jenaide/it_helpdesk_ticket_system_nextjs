import { Card, CardHeader, CardTitle } from "../ui/card";

export async function TicketComments({ ticketId }) {
    return (
        <Card>
            <CardHeader>
                <CardTitle>Comments</CardTitle>
            </CardHeader>
        </Card>
    )
}